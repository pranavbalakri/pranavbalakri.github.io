{
  "name": "pranavbalakri",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:articles": "node scripts/build-articles.js",
    "build": "npm run build:articles",
    "start": "npm run build:articles && npx serve public"
  }
}
